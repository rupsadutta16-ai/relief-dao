import { createThirdwebClient } from "thirdweb";

// For frontend apps, use the Client ID.
// Secret Key is only used for server-side operations.
export const client = createThirdwebClient({
  clientId: "7947017698e09ce73c72372a8369620d",
});
